module.exports = {
  // 用于检查用户是否满足更新
  // params: appVersion - app的版本号
  //         extraInfo  - 附加信息,json对象,来自app提交的数据
  // return: groupName  - 包所属的分组名,默认分组名为""
  updateCheck: function(appVersion, extraInfo){
    // todo: 检查用户是否满足更新
    return "";
  }
}
